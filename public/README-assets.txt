Drop these files in /public (all optional except the CV that already lives here):

  photo.jpg            your portrait (portrait orientation, ~1200×1500 works well)
  photo-cutout.png     OPTIONAL — same framing as photo.jpg, background removed (transparent PNG).
                       When present, the hero switches to a true layered 3D composition.
  Ahmed_Elshahat_Frontend_Developer.pdf   your CV (Download CV buttons point here)
  projects/*.png       project screenshots — then set `image: 'projects/xxx.png'` in src/content.js
